<Window
        xmlns:p="clr-namespace:OSEMAddIn.Properties" x:Class="OSEMAddIn.Views.Llm.LlmSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="LLM 设置"
        Height="320"
        Width="480"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility" />
    </Window.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="12" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Text="{x:Static p:Resources.Scope}" />
        <ComboBox Grid.Row="0" Grid.Column="2"
                  ItemsSource="{Binding ScopeOptions}"
                  SelectedItem="{Binding SelectedScope}">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Display}" />
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>

        <TextBlock Grid.Row="1" Grid.Column="0" Margin="0 12 0 0" VerticalAlignment="Center" Text="{x:Static p:Resources.Provider}" />
        <ComboBox Grid.Row="1" Grid.Column="2" Margin="0 12 0 0"
                  ItemsSource="{Binding ProviderOptions}"
                  SelectedItem="{Binding SelectedProvider}">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Display}" />
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>

        <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" Margin="0 12 0 0"
                    Visibility="{Binding IsOllama, Converter={StaticResource BoolToVisibility}}">
            <TextBlock Text="{x:Static p:Resources.Ollama_Model}" />
            <ComboBox Margin="0 4 0 0"
                      ItemsSource="{Binding AvailableOllamaModels}"
                      IsEditable="True"
                      Text="{Binding ModelName, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock Margin="0 4 0 0" FontSize="11" Foreground="Gray" Text="{x:Static p:Resources.Enter_model_name_directly_e_g_llama3_8b}"></TextBlock>
        </StackPanel>

        <StackPanel Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="3" Margin="0 12 0 0"
                    Visibility="{Binding ShowApiFields, Converter={StaticResource BoolToVisibility}}">
            <TextBlock Text="API Endpoint" />
            <TextBox Margin="0 4 0 0" Text="{Binding ApiEndpoint, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock Margin="0 8 0 0" Text="API Key" />
            <TextBox Margin="0 4 0 0" Text="{Binding ApiKey, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock Margin="0 4 0 0" FontSize="11" Foreground="Gray" Text="{x:Static p:Resources.Endpoint_e_g_https_api_example_8affd9}" />
        </StackPanel>

        <StackPanel Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="3" Margin="0 12 0 0"
                    Visibility="{Binding ShowApiFields, Converter={StaticResource BoolToVisibility}}">
            <TextBlock Text="{x:Static p:Resources.Model_Name}" />
            <TextBox Margin="0 4 0 0" Text="{Binding ModelName, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock Margin="0 4 0 0" FontSize="11" Foreground="Gray" Text="{x:Static p:Resources.Model_ID_used_for_requests}" />
        </StackPanel>

        <StackPanel Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 16 0 0">
            <Button Content="{x:Static p:Resources.Clear_Template_Override}" Margin="0 0 12 0" Click="OnClearTemplateClicked" IsEnabled="{Binding CanClearTemplateOverride}" />
            <Button Content="{x:Static p:Resources.Save}" Width="80" Margin="0 0 12 0" Click="OnSaveClicked" />
            <Button Content="{x:Static p:Resources.Close}" Width="80" Click="OnCloseClicked" />
        </StackPanel>
    </Grid>
</Window>
