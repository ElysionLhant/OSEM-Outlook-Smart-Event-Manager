<Window
        xmlns:p="clr-namespace:OSEMAddIn.Properties" x:Class="OSEMAddIn.Views.EventManager.TemplateRuleManagerView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{x:Static p:Resources.Template_Rule_Manager}" Height="450" Width="600" WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DataGrid Grid.Row="0" ItemsSource="{Binding Rules}" SelectedItem="{Binding SelectedRule}" AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="{x:Static p:Resources.Participants_Emails_semicolon_separated}" Binding="{Binding Participant}" Width="*" />
                <DataGridTemplateColumn Header="{x:Static p:Resources.Preferred_Template}" Width="200">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding SelectedTemplate.DisplayName}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                    <DataGridTemplateColumn.CellEditingTemplate>
                        <DataTemplate>
                            <ComboBox ItemsSource="{Binding DataContext.Templates, RelativeSource={RelativeSource AncestorType=Window}}"
                                      SelectedItem="{Binding SelectedTemplate}"
                                      DisplayMemberPath="DisplayName" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellEditingTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="{x:Static p:Resources.Add_Rule}" Command="{Binding AddRuleCommand}" Width="100" Margin="0,0,10,0" />
            <Button Content="{x:Static p:Resources.Delete_Rule}" Command="{Binding RemoveRuleCommand}" Width="100" Margin="0,0,10,0" />
            <Button Content="{x:Static p:Resources.Save_and_Close}" Command="{Binding SaveCommand}" Click="SaveButton_Click" Width="100" />
        </StackPanel>
    </Grid>
</Window>
